//-----------------------------------------------------------------------------
// Output depth to the shadow map RT
//-----------------------------------------------------------------------------

ps.2.0

// Incoming depth
dcl t0.x

mov r0, t0.x
mov oC0, r0